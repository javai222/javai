package com.dolormente.app.repository;

import com.dolormente.app.entity.ProductChip;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProductChipRepository extends JpaRepository<ProductChip, Long> {
}